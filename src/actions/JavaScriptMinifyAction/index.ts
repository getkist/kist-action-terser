// ============================================================================
// Import
// ============================================================================

import { JavaScriptMinifyAction, JavaScriptMinifyActionOptions } from "./JavaScriptMinifyAction.js";

// ============================================================================
// Export
// ============================================================================

/**
 * Barrel export for the JavaScriptMinifyAction module, re-exporting the
 * action class and its options type so consumers can import both from a
 * single module path (`./actions/JavaScriptMinifyAction/index.js`).
 */
export { JavaScriptMinifyAction, JavaScriptMinifyActionOptions };
